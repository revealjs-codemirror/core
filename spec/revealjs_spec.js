/*global describe:false, it:false, expect:false, Reveal */
describe('reveal.js', function(){
    var namespace = Reveal;

    it('should have a namespace', function(){
        expect(namespace).toBeDefined();
    });
});
